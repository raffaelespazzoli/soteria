# VolumeReplicationClass for Rook-Ceph RBD snapshot-based mirroring.
# Referenced by Soteria DRPlans as volumeReplicationClass: rook-ceph-rbd-vrc.
# schedulingInterval: 1m for fast RPO during integration testing.
apiVersion: replication.storage.openshift.io/v1alpha1
kind: VolumeReplicationClass
metadata:
  name: rook-ceph-rbd-vrc
spec:
  provisioner: rook-ceph.rbd.csi.ceph.com
  parameters:
    mirroringMode: snapshot
    schedulingInterval: "1m"
    replication.storage.openshift.io/replication-secret-name: rook-csi-rbd-provisioner
    replication.storage.openshift.io/replication-secret-namespace: rook-ceph

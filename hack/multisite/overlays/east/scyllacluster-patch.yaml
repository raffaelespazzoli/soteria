# East is the seed DC — it bootstraps first with no external seeds.
# After west joins, setup-scylladb.sh patches east to add the west
# seed for symmetric reconnection after network partitions.
- op: replace
  path: /spec/datacenter/name
  value: east
- op: remove
  path: /spec/externalSeeds

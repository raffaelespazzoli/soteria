# East discovers west via Cilium Cluster Mesh DNS.
# Symmetric seeds: both DCs can survive independently and rejoin.
- op: replace
  path: /spec/datacenter/name
  value: east
- op: replace
  path: /spec/externalSeeds
  value:
    - soteria-scylladb-west-rack1-0.soteria.svc.clusterset.local

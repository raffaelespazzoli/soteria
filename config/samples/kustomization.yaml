apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - soteria_v1alpha1_drplan.yaml
  # soteria_v1alpha1_drexecution.yaml is a reference example only — DRExecution
  # objects are created by the controller, not applied directly.
  - vm_with_drplan_label.yaml

# Placeholder: PR checks for the Soteria operator
name: PR Operator Checks
on:
  pull_request:
    branches: [main]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO - implement PR checks"

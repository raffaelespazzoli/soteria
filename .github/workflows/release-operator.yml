# Placeholder: Release workflow for the Soteria operator
name: Release Operator
on:
  push:
    tags: ["v*"]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO - implement release workflow"

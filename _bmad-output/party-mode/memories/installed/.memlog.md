---
topic: Soteria planned-migration Step 0 sync wait
goal: Confirm whether the sync wait was removed and plan the code fix if the wait is leftover
updated: 2026-09-04T19:26
---

- (moment) Raffa walked in from a stuck planned migration: east VMs halted, both sites secondary, engine looping on Demoted VRs not yet healthy.
- (outcome by bmad-agent-dev) waitForSync is gone from Go. The wait moved into drexecution checkVRsHealthy: RoleTarget AND HealthHealthy. CSI Degraded from missing lastSyncTime never converges. Multi-site Step 0 has no timeout on that wait.
- (moment by bmad-agent-pm) Raffa wants Amelia's plan. Fight is process: not a new epic. 15.11 AC2 is the health wait they just hit. Spawn 15.13 vs quick-dev vs course-correct FR9.
- (outcome) Raffa confirmed: since ResyncVolume is gone, VR will never reach Healthy after demotion — Healthy requires Completed=True which requires lastSyncTime which requires an active resync. Demotion just flips state. Creating 15.13 to remove the health gate.
- (outcome) Story 15.13 created: Remove Demotion Health Gate from Step 0. Core fix is one condition change in checkVRsHealthy. Sprint status updated. Ready-for-dev.
- (outcome) Party ended. Raffa deployed 15.13 patch overnight. Failover completed but VMs didn't start on target. Resuming diagnostics outside party mode.
- (dynamic) Raffa back with a new bug: PreExecute shuts down VMs in ascending wave order (db first), same as startup. Should be reverse for graceful shutdown (webservers first, db last). Discussing complexity and whether it needs a story.
- (outcome) Reverse-wave shutdown fix shipped as quick-dev: slices.Reverse(uniqueVMs) in PreExecute. New test TestFailoverHandler_Graceful_Step0_ReverseWaveShutdownOrder asserts webservers stop before db. All 85 engine tests pass. Room agreed no story needed.
- (dynamic) Raffa brings packaging + docs to the room. Wants OLM/OperatorHub + Helm chart following vault-config-operator pattern and redhat-cop pipeline. Also wants extensive docs: install prereqs (CSI addons, VolumeReplication, ScyllaDB networking), architecture (two-cluster topology, 4-state DR transitions), and usage (waves, throttling, namespace vs VM grouping, UI screenshots).
- (moment) Raffa clarifies: OLM is for OCP users, Helm is for non-OCP Kubernetes users. Both are end-user distribution channels. Pipeline serves both. helmchart-test just needs to confirm deploy succeeds, not test runtime. Wants minimal ScyllaDB in helmchart-test. Workstream 1 (packaging + CI) goes first.
- (moment) Raffa asks if console plugin and standalone UI can be one image. Room discovers the architecture already has a clean K8sProvider abstraction — two webpack builds, two runtimes (nginx vs console-proxy Go binary), but shared components. Consolidation is feasible at the Docker/entrypoint level, not a major refactor.
- (outcome) Decision: two images (console-plugin nginx + standalone Go proxy). Standalone UI is Helm-only option, not in OLM bundle. OLM bundle ships controller + console plugin only. Now diving into John's 7-gap plan for pipeline conformance.
- (dynamic) Raffa pivots: OLM and redhat-cop pipeline shelved. New goal is 'anyone can quickly try this operator.' Helm chart that deploys everything including ScyllaDB with three networking approaches (Submariner, Cilium, cUDN localnet). Simple release pipeline: build images, push quay, publish Helm chart to GitHub Pages.
- (outcome) Decisions: (1) Real Helm templating, not kustomize dump. (2) Cert-manager + Issuer are prerequisites — chart only creates Certificates. No cert-manager sub-chart. (3) Discussing umbrella vs flat chart structure.
- (outcome) cUDN localnet clarified: secondary network for internode traffic, pod network stays for client CQL. ScyllaDB dual-NIC config: listen_address=cUDN IP (internode), rpc_address=pod IP (client). Static IPs passed as params, set via k8s.v1.cni.cncf.io/networks annotation. No NAD creation needed. UDN pre-exists. Seeds are raw IPs not FQDNs. Certs must cover those IPs in SANs.
- (outcome) cUDN localnet mode shelved as future TODO due to scylla-operator per-pod annotation limitations. Added 'bring your own ScyllaDB' mode: user provides contact points, keyspace, TLS secrets. Chart has three ScyllaDB modes: managed (operator), external (BYO), and future cUDN.
- (outcome) Final values.yaml and 8-item work plan locked. ScyllaDB has managed/external modes. Networking is cilium/submariner (cUDN deferred). UI is console-plugin/standalone/none. Release pipeline: 3 images to quay + chart-releaser to GH Pages. Raffa approved the plan.
- (dynamic) Moving to docs workstream discussion, then capturing everything (packaging + docs) as epics and stories.
- (outcome) Docs methodology: PRD + architecture docs are the base, but each page must cross-reference implemented stories and actual code to capture current behavior (not aspirational specs). Screenshots will come from a live Soteria UI instance Raffa will provide access to.
- (outcome) Creating Epic 16 (Helm Chart & Release Pipeline, 8 stories) and Epic 17 (Documentation Site, 22 stories) as story files. Party wrapping up after file creation.
